<!-- ╔══════════════════════════════════════════════════════════╗
     ║  STUDIO TABI — GLOBAL HEAD                               ║
     ║  Elementor → Custom Code → Before </head>               ║
     ╚══════════════════════════════════════════════════════════╝ -->
<link rel="preconnect" href="https://fonts.googleapis.com" />
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
<link href="https://fonts.googleapis.com/css2?family=Noto+Serif+JP:wght@900&family=Roboto+Condensed:wght@700;900&family=Be+Vietnam+Pro:wght@400;500;600&display=swap" rel="stylesheet" />
<style>
  :root {
    --tabi-red:   #F20C25;
    --tabi-white: #EFEFEF;
    --tabi-black: #111111;
    --tabi-pad:   clamp(20px, 4vw, 82px);
  }
  /* Utility reveal */
  .tabi-reveal { opacity: 0; transform: translateY(24px); transition: opacity .85s cubic-bezier(.16,1,.3,1), transform .85s cubic-bezier(.16,1,.3,1); }
  .tabi-reveal.is-visible { opacity: 1; transform: translateY(0); }
  /* Clip-up headline */
  .tabi-hl-wrap { overflow: hidden; line-height: .88; display: block; }
  .tabi-hl-line { display: block; transform: translateY(110%); transition: transform .9s cubic-bezier(.16,1,.3,1); }
  .tabi-hl-line.is-visible { transform: translateY(0); }
  .tabi-hl-line--red { color: var(--tabi-red); }
  /* Pillar / Stat bar wipe */
  .tabi-bar-fill { position:absolute;inset:0;transform-origin:left;transform:scaleX(0);transition:transform .6s cubic-bezier(.16,1,.3,1); }
  .tabi-bar-fill.is-visible { transform:scaleX(1); }
</style>