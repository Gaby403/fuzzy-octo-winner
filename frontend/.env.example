# URL base da instalação WordPress headless (SEM barra no final).
# Ex. em produção:  https://cms.seudominio.com.br
# Ex. no dev local (docker-compose):  http://localhost:8080
#
# Se ficar vazio, o site usa o conteúdo padrão embutido (modo offline/preview).
VITE_WP_API=http://localhost:8080
